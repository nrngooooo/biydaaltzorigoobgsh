@model IEnumerable<biydaalt.Models.Actshaltgaan>

@{
    ViewData["Title"] = "Актласан шалтгаан нүүр хуудас";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<h1>Нүүр хуудас</h1>

<p>
    <a asp-action="Create">Шинээр актласан шалтгаан нэмэх</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.actshaltgaanName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.actshaltgaanName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.actshaltgaanId">Засах</a> |
                <a asp-action="Details" asp-route-id="@item.actshaltgaanId">Дэлгэрэнгүй</a> |
                <a asp-action="Delete" asp-route-id="@item.actshaltgaanId">Устгах</a>
            </td>
        </tr>
}
    </tbody>
</table>
