@model IEnumerable<biydaalt.Models.Bookact>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.book)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.worker)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.act_count)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.actshaltgaan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.actdate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.book.bookId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.worker.workerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.act_count)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.actshaltgaan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.actdate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.bookactId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.bookactId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.bookactId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
