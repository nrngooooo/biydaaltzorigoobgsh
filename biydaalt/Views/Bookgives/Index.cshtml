@model IEnumerable<biydaalt.Models.Bookgive>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.book)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.user)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.worker)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.enterdate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.retdate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.book.bookId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user.userId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.worker.workerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.enterdate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.retdate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.bookgiveId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.bookgiveId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.bookgiveId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
