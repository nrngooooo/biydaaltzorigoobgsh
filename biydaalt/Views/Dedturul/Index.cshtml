@model IEnumerable<biydaalt.Models.Dedturul>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dedTurulName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.turul)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dedTurulName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.turul.turulId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.dedTurulId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.dedTurulId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.dedTurulId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
