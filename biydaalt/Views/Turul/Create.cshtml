@model biydaalt.Models.Turul

@{
    ViewData["Title"] = "Номын төрөл нэмэх";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<h1>Номын төрөл нэмэх</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="turulName" class="control-label"></label>
                <input asp-for="turulName" class="form-control" />
                <span asp-validation-for="turulName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Нэмэх" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Буцах</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
