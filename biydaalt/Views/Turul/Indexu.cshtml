@model IEnumerable<biydaalt.Models.Turul>

@{
    ViewData["Title"] = "Indexu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Indexu</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.turulName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.turulName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.turulId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.turulId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.turulId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
